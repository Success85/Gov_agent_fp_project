"""
test_setup.py
-------------
LANE 5 - Step 1.
This is the simplest possible test. Its only purpose is to confirm that pytest
is installed and running correctly. If this passes (goes green), your testing
framework is set up. This can be written and run BEFORE the backend exists.

Run all tests from the backend/ folder with:  pytest
"""


def test_pytest_is_working():
    # A test "passes" if the assert statement is true.
    assert 1 + 1 == 2